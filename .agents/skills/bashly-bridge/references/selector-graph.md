# Selector Graph

The selector graph and projection boundaries are defined in:

- `docs/contracts/bashly-bridge.md`
- `docs/contracts/diagnostic-loop.md`

Use these docs as the authoritative source for:

- Bashly command intent selectors
- source selectors
- joined projection rules
- selector-addressed diagnostics
