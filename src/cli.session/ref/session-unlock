#!/usr/bin/env bash
set -euo pipefail

session_bin="${SESSION_BIN:-session}"

if ! command -v "$session_bin" >/dev/null 2>&1; then
  fallback="$HOME/.local/bin/session"
  if [[ -x "$fallback" ]]; then
    session_bin="$fallback"
  fi
fi

exec "$session_bin" unlock "$@"
