#!/usr/bin/env bash
set -euo pipefail

project=${1:-.}
cd "$project"

if [[ -f bashly-settings.yml || -f settings.yml || -f bashly.yml || -f src/bashly.yml ]]; then
  exec bashly generate
fi

printf 'error: %s does not look like a Bashly project root\n' "$project" >&2
exit 2
