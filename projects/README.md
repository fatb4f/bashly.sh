# Projects

Place Bashly project roots here.

A project root usually contains one or more of:

```txt
bashly.yml
src/bashly.yml
bashly-settings.yml
settings.yml
```

`projects/example-cli/` is a minimal placeholder layout.
