# ACP Integration

ACP is an optional transport and provider layer that can consume the Bashly
bridge.

It may be used to route actions into Neovim, but it does not define the bridge
authority model.

The bridge still owns:

- selector graphs
- projection rules
- diagnostics and gates
- headless Neovim mutation authority
